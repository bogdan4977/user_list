import React from 'react';
import {  } from 'prop-types';



// const { all, done, process } = filters;

const VideoListItem = ({info}) => (
    <li>
        {info}
    </li>
);

VideoListItem.propTypes = {

};

export default VideoListItem;